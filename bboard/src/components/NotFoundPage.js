import React, { PropTypes } from 'react'

const NotFoundPage = React.createClass({
    render () {
        return <div>
            Not found page
        </div>
    }
})

export default NotFoundPage;
